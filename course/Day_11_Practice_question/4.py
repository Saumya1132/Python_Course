# using numpy array find sum and product of its elements

import numpy as np

arr = np.array([[1, 2, 3], [4, 5, 6]])
sum_elements = np.sum(arr)
product_elements = np.prod(arr)

print("Sum of elements:", sum_elements)

print("Product of elements:", product_elements)