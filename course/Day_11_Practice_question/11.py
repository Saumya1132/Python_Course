# calculate the dot product of the array itself

import numpy as np

# Example array
array = np.array([1, 2, 3, 4, 5])

dot_product = np.dot(array, array)

print(f"The dot product of the array with itself is: {dot_product}")
